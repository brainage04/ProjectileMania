package com.github.brainage04.projectilemania.command;

import net.fabricmc.fabric.api.command.v2.CommandRegistrationCallback;
import net.minecraft.text.Text;

public class ModCommands {
    public static void registerCommands() {
/*
        CommandRegistrationCallback.EVENT.register((dispatcher, registryAccess, environment) -> dispatcher.register(Text.literal("test")
                .executes(context -> {
                    context.getSource().sendFeedback(() -> Text.literal("Called /foo with no arguments"), false);

                    return 1;
                }))
        );
 */
    }
}
